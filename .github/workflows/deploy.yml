name: Публикация сайта

# Сайт пересобирается и публикуется сам при каждом изменении в ветке main.
# Это значит, что текст можно править прямо на сайте github.com, без установки
# чего-либо на компьютер: изменили content.json, нажали «Commit» — через пару
# минут сайт обновился.

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    name: Сборка
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      # npm ci, а не npm install: ставит зависимости строго по package-lock.json.
      # Значит, на GitHub собирается ровно то же, что проверено локально.
      - run: npm ci
      - run: npm run build

      - uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

  deploy:
    name: Публикация
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
